# SI Research Report

This repository generates Markdown, CSV, and JSON reports after a worker processes a task. Run:

```bash
python -m app.run_pending_tasks
```

Reports are written to the configured `REPORT_OUTPUT_DIR`.

